# 读书笔记生成约定

> 本仓库生成的是适合戴耳机收听的核心内容朗读稿。目标是在约三十分钟内，用自然、连贯的中文详细讲清一本书的核心问题、结论、论证、概念和边界，而不是制作视觉化分析表或章节摘要。最后更新：2026-09-14。

## 一、触发与授权

以下输入触发完整工作流：

- 用户消息仅包含一个或多个书名；
- 用户明确要求生成某本书的读书笔记、精读稿或朗读稿。

单独发送书名即表示用户授权本次生成文件、更新 `README.md`、创建 Git commit，并推送到 `origin/main`，无需中途重复确认。

普通问答不触发自动生成。例如“《原则》讲了什么？”只回答问题；“生成《原则》朗读稿”才执行本约定。

以下情况必须暂停并询问：

- 同名书、作者或版本存在歧义；
- 仓库中已有同一本书，需要确认是重写还是取消；
- 目标笔记或 `README.md` 存在未提交修改；
- 当前分支、远端状态或认证不满足安全推送条件；
- 资料不足以可靠还原核心内容。

## 二、内容目标

一篇合格朗读稿必须让听众听懂五件事：

1. 这本书试图解决什么问题；
2. 作者给出的核心答案是什么；
3. 作者如何一步步得出这个答案；
4. 哪些概念支撑了整套论证；
5. 结论在什么条件下成立，又可能在哪里失效。

取舍原则：

- **核心优先**：只保留能够解释全书主线的内容，不追求章节覆盖率。
- **论证优先**：不仅讲作者说了什么，还要讲为什么、凭什么以及如何推出。
- **机制优先**：案例只用于解释机制，不讲与主线无关的故事。
- **边界优先**：重要观点必须说明适用条件、反例或潜在副作用。
- **准确优先**：宁可承认资料不足，也不补写无法核实的信息。
- **听感优先**：同一意思只说一次，删除不影响理解的背景、术语和格式提示。

禁止事项：

- 不把目录改写成精读稿；
- 不从书名反推作者观点；
- 不编造引语、页码、章节、案例、实验或数据；
- 不把整理者推论写成作者结论；
- 不用固定条数制造弱相关内容；
- 未读取原书时不声称已经通读原书。

## 三、资料核验只在内部进行

### 1. 资料模式

生成前在内部选择资料模式，不写入报告。

- **原书精读模式**：用户提供了足以覆盖主要论证的原书、章节、划线或详细笔记。
- **公开资料研读模式**：用户只提供书名。至少交叉核对一个一手或官方来源，以及两个独立可靠来源。
- **资料不足**：无法确认书籍、只有重复营销文案、可靠来源相互冲突，或无法形成可追溯的论证链。此时停止生成。

### 2. 证据管理

生成者在内部区分原书观点、书中案例、作者公开阐述、公开资料归纳、整理者推论和自拟示例，但这些标签不写入朗读稿。

只有可靠一手材料支持时，才能把观点或案例归于作者。自拟示例必须符合原书逻辑，且在叙述中自然说明它只是用于理解的例子。

在线资料以转述为主，不大段复制。必须归因的关键事实，将链接嵌入正常词语，不显示裸网址，不设置参考资料章节。

### 3. 资料充分度

在内部评估，不写入报告：

- **高**：原书主要内容可用，并能与权威来源相互核对；
- **中**：官方资料、作者阐述和多个可靠来源足以还原主线，但未阅读全文；
- **低**：不足以可靠生成，停止任务。

## 四、执行流程

### 1. Git 预检

1. 运行 `git status --short --branch`，记录初始状态。
2. 确认当前分支为 `main`，远程仓库为 `origin`。
3. 获取 `origin/main` 并比较：远端领先或分叉时停止，不自动 merge、rebase 或强推。
4. 未提交的无关文件不得暂存；目标笔记或 `README.md` 已修改时停止并询问。

### 2. 识别与查重

1. 内部确认正式书名、作者、原文名、版本和内容类型。
2. 使用“规范化书名加作者”检查 `README.md` 和 `notes/`。
3. 同名书或版本差异会影响内容时先询问，不猜测。
4. 确定资料模式和充分度，等级为低时停止。

### 3. 编号和文件名

1. 只扫描符合 `notes/^(\d+)_.*\.md$/` 的文件。
2. 取最大编号加一，不复用空号；不足两位时补零。
3. 每本书单独生成一份文件，多本书使用连续编号并合并为一个 commit。
4. 文件名格式为 `notes/NN_简洁书名.md`。
5. 文件名去掉书名号、副标题和非法路径字符，正文保留正式书名。
6. `notes/` 文件名是编号的唯一事实来源。

### 4. 写作前提炼

正式写作前，先在内部完成：

1. 用一个问句定义核心问题；
2. 用一句可被反驳的判断表达核心答案；
3. 整理五到八个连续的论证步骤；
4. 选择五到八个不可缺少的核心概念；
5. 选择六到八条解释力最高的关键洞见；
6. 为洞见核对推理、依据、例子和适用边界；
7. 找出四到六个反例、争议或遗漏；
8. 执行压缩测试：删除后不影响主线理解的内容，不写入正文。

### 5. 更新索引

只修改 `README.md` 中 `<!-- NOTES_INDEX_START -->` 与 `<!-- NOTES_INDEX_END -->` 之间的表格，按编号升序添加相对链接。

## 五、朗读稿格式

报告固定包含以下六个二级标题，顺序不得改变：

1. `书籍信息`
2. `核心问题与结论`
3. `论证主线`
4. `核心概念`
5. `关键洞见`
6. `局限与边界`

一级标题只写 `# 《书名》`。书籍信息只写书名和作者，不展示国别、背景、类型、版本、资料模式、充分度或阅读目的。

### 朗读表达要求

- 正文通常为八千到九千个中文字符，按每分钟约二百八十字计算，对应约二十九到三十二分钟；内容复杂时可略长，内容简单时可以略短。
- 建议分配：核心问题与结论八百到一千一百字；论证主线一千五百到两千字；五到八个核心概念，每个二百五十到四百字；六到八条关键洞见，每条三百五十到五百五十字；四到六个局限，每个二百到三百五十字。
- 扩展内容必须增加推理步骤、概念联系、案例、反例或成立条件，不能通过重复结论、堆砌作者背景或拉长句子凑时长。
- 使用自然口语和完整句子，像一个人把书讲给另一个人听。
- 每段只讲一个意思，通常两到四句话；单句尽量不超过五十个中文字符。
- 使用“先看”“接下来”“这意味着”“但要注意”等自然过渡，避免观点突然跳转。
- 论证主线用“第一步、第二步”连续讲述，不使用代码树、流程图或箭头。
- 核心概念先给白话定义，再说明它在全书中的作用；不要重复朗读“定义、关系、常见误解”等字段名。
- 关键洞见将主张、理由、例子和边界融入连续段落；不显示证据标签。
- 数字、缩写和英文只在理解所必需时出现，首次出现时用中文解释。
- Markdown 只用于标题和必要链接，不使用表格、代码块、嵌套列表、装饰性括号或裸网址。

### 不应出现在朗读稿中的内容

- `【】` 等装饰性标题；
- “结构依据、资料模式、资料充分度、覆盖范围”等制作说明；
- `[原书观点]`、`[公开资料归纳]`、`[自拟示例]` 等证据标签；
- 行动清单、主动回忆问题、参考资料、延伸阅读；
- 报告生成时间、合规提示、分隔线；
- 与主线无关的作者履历、出版信息和推荐语；
- 同一观点在主线、概念和洞见中机械重复。

## 六、质量验收

提交前检查：

- 文件名、编号和 README 链接一致；
- 六个二级标题存在且顺序正确；
- 正文原则上为八千到九千个中文字符，预计朗读时间接近三十分钟；
- 书籍信息只含书名和作者；
- 核心问题只有一个，一句话答案清楚且可检验；
- 论证主线包含三到六个连续步骤，不是观点列表；
- 核心概念和关键洞见都服务于主线；
- 重要观点包含理由和边界，但没有重复字段标签；
- 作者观点、整理者推论与示例在语义上清楚分离；
- 不存在未经核实的引语、页码、数据或案例；
- 不包含代码块、表格、证据标签、行动、问答、参考资料、延伸阅读或生成时间；
- `git diff --check` 通过；
- 暂存区只包含本次新笔记、`README.md` 和用户明确要求的文件。

内部朗读测试：从头顺读一遍，确认没有需要“看屏幕”才能理解的内容，没有连续重复的格式词，也没有突兀的链接、符号或英文串。

## 七、Git 提交与推送

远程仓库为 `origin`，目标分支为 `main`。

单本书：

```bash
git add -- notes/NN_书名.md README.md
git diff --cached --check
git commit -m "add notes: 《书名》精读报告 (NN)"
git push origin main
```

多本书必须逐个列出新增文件，禁止使用 `git add .` 或 `git add -A`。提交前检查 staged diff，不跳过 hooks，不强推。

推送后确认本地 `HEAD`、`origin/main` 和远端分支指向同一 commit。

失败处理：

- commit 失败时保留修改并报告；
- hook 失败时修复后重新暂存，不使用 `--no-verify`；
- push 失败时保留本地 commit，不反复重试；
- non-fast-forward 时停止，不自动 pull、merge、rebase 或强推。

## 八、完成回复

完成时返回文件路径、编号、内部资料充分度、朗读与 Markdown 校验结果、commit hash、推送目标和同步状态。失败时说明完成到哪一步，以及是否产生本地 commit。

## 九、历史版本

- `01` 到 `17` 为早期导入版本。
- `18` 到 `24` 为包含案例解释的结构化版本。
- 从 `25` 开始使用面向 AI 朗读的核心内容格式。
- 除非用户明确要求，不在新增笔记时批量改写历史文件。
